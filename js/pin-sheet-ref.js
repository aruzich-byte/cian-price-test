// pin-sheet.js создаётся в main.js после карточек/карты, но карточки (карусель) и
// карта должны уметь открывать его по клику. Общая мутируемая ссылка вместо
// отдельной глобальной переменной — main.js проставляет .current после инициализации.
window.App = window.App || {};
window.App.pinSheetRef = { current: null };
